import React from "react";
import { Text, View } from "react-native";

const Deliveries = () => {
  return (
  <View style={{flex:1,alignItems:'center',justifyContent:'center'}}>
        <Text style={{fontSize:25}}>Deliveries</Text>
  </View>);
};

export default Deliveries;